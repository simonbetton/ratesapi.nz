# Implementation Plans

Generated by the `improve` skill on 2026-09-24 against commit `5c9ae82`, from the user's selection of findings 1–5. These files are handoffs for an executor with no prior conversation context. Read each plan fully, honor its drift and STOP checks, and update its status row when complete. Source code has not been changed by the advisor.

## Execution order and status

| Plan | Outcome | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| [001](001-install-web-dependencies-in-ci.md) | Install web dependencies in shared CI | P1 | S | — | TODO |
| [002](002-reject-empty-scrapes.md) | Reject empty or structurally broken scrapes | P1 | M | — | TODO |
| [003](003-fail-scraper-jobs-on-error.md) | Fail scheduled jobs on scraper errors | P1 | M | 002 | TODO |
| [004](004-preserve-zero-credit-card-values.md) | Preserve zero-valued credit-card numbers | P1 | S | 002, 003 | TODO |
| [005](005-support-unicode-snapshots.md) | Serialize Unicode without breaking old blobs | P1 | S | — | TODO |

Status values: TODO, IN PROGRESS, DONE, BLOCKED (include reason), REJECTED (include rationale).

## Dependency notes

- 001 restores a meaningful clean-runner check before changing data collection. It is operationally first, though it does not change the scraper files.
- 002's ingestion guard must be in place before 003 rewires the same entrypoints to propagate its errors.
- 004 follows 002 and 003 because all three edit the credit-card entrypoint. 005 is independent and can run after 001 in parallel with scraper work if separate worktrees are used.
- Each plan is otherwise self-contained: do not assume an executor has read another plan's text. Check source drift before work because the repository contained unrelated uncommitted web changes when these plans were written.

## Verification baseline

At audit time, `bun test` passed 27 tests; root, docs, and web TypeScript checks passed. `bun run lint` failed on a pre-existing import-order issue in `apps/web/vite.config.ts`. The plans must not expand scope to fix that unrelated file. The root dependency vulnerability audit was not completed because automatic approval review rejected sending package metadata externally; no dependency finding is included in these plans.

## Findings considered and rejected

- Open CORS and unauthenticated API access: intentional public-service contract in `CASE-STUDY.md`; not a vulnerability finding.
- Daily historical replacement and skip-unchanged writes: intentional storage policy; the mismatch between sparse change-day history and some copy is a separate documentation opportunity.
- JSON document snapshots in D1 and MCP exclusion from OpenAPI: documented architecture decisions, not refactor findings.
- HTTP retry helper behavior: current scrapers do not configure retries, so this is lower leverage than the selected work.
