ENVIRONMENT=development

# Convex deployment URL for API reads and script writes.
CONVEX_URL=https://<your-deployment>.convex.cloud

# Shared secret used by scrapers/migration to write snapshots.
CONVEX_INGEST_SECRET=<your-long-random-secret>

# Cloudflare D1 database name or binding used by wrangler d1 execute.
CLOUDFLARE_D1_DATABASE=ratesapi-data
# Alternatives also supported by local migration helpers:
# D1_DATABASE_NAME=ratesapi-data
# CLOUDFLARE_D1_DATABASE_ID=<database-id>

# Optional: lower this if migration output is very large.
# D1_MIGRATION_PAGE_SIZE=100
